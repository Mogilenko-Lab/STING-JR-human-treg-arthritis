# 02_eda — Unused

This stage number is reserved and carries no artifacts. Its `figures/` and `tables/` directories
hold a `.gitkeep` apiece.

The exploratory work this slot was scaffolded for lives elsewhere. The unsupervised embedding and
the mitochondrial-pocket characterisation are in [`../01_qc/`](../01_qc/). The sort-label audit is
in [`../02_annotation/`](../02_annotation/). The pseudobulk principal-component view is in
[`../03_pseudobulk/`](../03_pseudobulk/).

The directory is kept so the stage numbering stays contiguous with the script numbering under
`02_analysis/scripts/`.
